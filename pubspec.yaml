name: gamepad_glyphs
description: "Display gamepad glyphs based on input type"
version: 0.0.1
homepage:

environment:
  sdk: ^3.12.2
  flutter: '>=3.3.0'

dependencies:
  flutter:
    sdk: flutter
  flutter_svg: ^2.0.17
  flutter_web_plugins:
    sdk: flutter
  web: ^1.0.0
  plugin_platform_interface: ^2.0.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec

# The following section is specific to Flutter packages.
flutter:
  assets:
    - assets/input_prompt/Arcade/
    - assets/input_prompt/Arcade/blue/
    - assets/input_prompt/Arcade/green/
    - assets/input_prompt/Arcade/yellow/
    - assets/input_prompt/Keyboard/
    - assets/input_prompt/Keyboard/Light/
    - assets/input_prompt/Keyboard/MonochromeDark/
    - assets/input_prompt/Keyboard/MonochromeLight/
    - assets/input_prompt/Luna/
    - assets/input_prompt/OUYA/
    - assets/input_prompt/PS Move/
    - assets/input_prompt/PS3/
    - assets/input_prompt/PS4/
    - assets/input_prompt/PS5/
    - assets/input_prompt/PS5/MonochromeDark/
    - assets/input_prompt/PSVita/
    - assets/input_prompt/Quest/
    - assets/input_prompt/Stadia/
    - assets/input_prompt/Steam (G1)/
    - assets/input_prompt/Switch Joy-Con/
    - assets/input_prompt/Wii/
    - assets/input_prompt/Xbox 360/
    - assets/input_prompt/Xbox One/
    - assets/input_prompt/Xbox Series X-S/
    - assets/input_prompt/Xbox Series X-S/MonochromeDark/

  # This section identifies this Flutter project as a plugin project.
  # The 'pluginClass' specifies the class (in Java, Kotlin, Swift, Objective-C, etc.)
  # which should be registered in the plugin registry. This is required for
  # using method channels.
  # The Android 'package' specifies package in which the registered class is.
  # This is required for using method channels on Android.
  # The 'ffiPlugin' specifies that native code should be built and bundled.
  # This is required for using `dart:ffi`.
  # All these are used by the tooling to maintain consistency when
  # adding or updating assets for this project.
  plugin:
    platforms:
      android:
        package: io.github.seanocali.gamepad_glyphs
        pluginClass: GamepadGlyphsPlugin
      ios:
        pluginClass: GamepadGlyphsPlugin
      linux:
        pluginClass: GamepadGlyphsPlugin
      macos:
        pluginClass: GamepadGlyphsPlugin
      windows:
        pluginClass: GamepadGlyphsPluginCApi
      web:
        pluginClass: GamepadGlyphsWeb
        fileName: gamepad_glyphs_web.dart

  # To add assets to your plugin package, add an assets section, like this:
  # assets:
  #   - images/a_dot_burr.jpeg
  #   - images/a_dot_ham.jpeg
  #
  # For details regarding assets in packages, see
  # https://flutter.dev/to/asset-from-package
  #
  # An image asset can refer to one or more resolution-specific "variants", see
  # https://flutter.dev/to/resolution-aware-images

  # To add custom fonts to your plugin package, add a fonts section here,
  # in this "flutter" section. Each entry in this list should have a
  # "family" key with the font family name, and a "fonts" key with a
  # list giving the asset and other descriptors for the font. For
  # example:
  # fonts:
  #   - family: Schyler
  #     fonts:
  #       - asset: fonts/Schyler-Regular.ttf
  #       - asset: fonts/Schyler-Italic.ttf
  #         style: italic
  #   - family: Trajan Pro
  #     fonts:
  #       - asset: fonts/TrajanPro.ttf
  #       - asset: fonts/TrajanPro_Bold.ttf
  #         weight: 700
  #
  # For details regarding fonts in packages, see
  # https://flutter.dev/to/font-from-package
